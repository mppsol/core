# mppsol/core (archived)

> **This repository has been consolidated into [`mppsol/sdk`](https://github.com/mppsol/sdk) on 2026-05-07.**

The `@mppsol/core` npm package continues to publish from the new location:

- **Code:** [mppsol/sdk/tree/main/packages/core](https://github.com/mppsol/sdk/tree/main/packages/core)
- **Issues:** [mppsol/sdk/issues](https://github.com/mppsol/sdk/issues)
- **npm:** [`@mppsol/core`](https://www.npmjs.com/package/@mppsol/core) — install instructions unchanged

Existing installations of older versions are not affected. The current published version is `0.1.0-draft.4` (and onward), published from the consolidated repo.

Git history was preserved into `packages/core/` of the new repo via `git filter-repo`. To find the original commit for any file, run `git log packages/core/<path>` in [mppsol/sdk](https://github.com/mppsol/sdk).
